   services:
     - type: web
       name: market-brief-api
       env: python
       buildCommand: pip install -r requirements.txt
       startCommand: uvicorn src.model.main:app --host 0.0.0.0 --port $PORT